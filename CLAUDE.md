# nntrainer project notes

## Git push rule

**Always push to `origin/all_gpu`.**

```
git push -u origin HEAD:all_gpu
```

Never push to `claude/...` or any other branch unless explicitly told otherwise.
